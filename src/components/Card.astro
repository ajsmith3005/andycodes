---
import '../styles/app.scss';
---

<div class="card is-style-centered-mobile">
	<slot />
</div>

<style lang="scss">
	@use '../styles/global-abstracts' as *;

	.card {
		display: flex;
		flex-wrap: wrap;
		gap: $spacing-10;
		background-color: $color-base-1;
		padding: $spacing-20;
		border-radius: $radius-1;
		box-shadow: 0px 4px 4px rgba(0, 0, 0, .25);
		width: 100%;
	}

</style>

