---
const { class: className, ...rest } = Astro.props
---

<div class=`buttons ${className}`>
	<slot />
</div>

<style lang="scss">
	@use '../styles/global-abstracts' as *;

	.buttons {
	display: flex;
	gap: $spacing-10;
	flex-wrap: wrap;
	}
</style>